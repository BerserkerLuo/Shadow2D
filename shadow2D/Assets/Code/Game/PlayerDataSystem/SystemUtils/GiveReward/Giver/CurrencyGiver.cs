

using Table;

namespace PlayerSystemData
{
    public class CurrencyGiver : BaseGiver
    {
        public void DoReward(int itemId, int itemCount)
        {
            //DataItem data = DataItemManager.Instance.GetDataByID(itemId);
            //if (data == null)
            //    return;

            //CurrencyUtils.TryChangeCurrency(data.SubType, itemCount * (int.Parse)(data.Value));

            ////switch (subType) {
            ////    case 金币:
            ////    case 金币:
            ////}
        }
    }
}
